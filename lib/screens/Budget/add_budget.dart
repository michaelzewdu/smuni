import 'package:flutter/material.dart';

class AddNewBudgetScreen extends StatelessWidget {
  const AddNewBudgetScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
